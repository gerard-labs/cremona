# Cremona component catalog

_Generated by `tools/gen-skill-catalog.js` — do not edit by hand. 122 components._

Every component lives at `src/templates/components/<slug>/` — a `<slug>.html.twig` (canonical markup, the source of truth) and a `<slug>.story.vue` (live demo). Components flagged **[JS]** ship a Stimulus controller — copy the `data-controller` / `data-action` / `data-*` attributes from the twig verbatim or they will be inert.

## Ring 1 — Primitives

- `accordion` — orchestrates N Collapsible items with optional single-mode mutex + roving tabindex + Arrow / Home / End keyboard navigation between triggers. **[JS: accordion]**
- `alert` — inline status banner with optional dismiss + action. **[JS: alert-dismiss]**
- `aspect-ratio` — box wrapper that maintains a defined aspect ratio for its inner content (typically an image, video, iframe, or canvas).
- `avatar` — image + initials fallback, 5 sizes, optional status dot, deterministic hue (1-6) via the KPI palette. **[JS: avatar-fallback]**
- `badge` — status / count chip.
- `breadcrumb` — navigation trail (you-are-here).
- `button` — native <button type="button"> or <a href> styled like a button.
- `button-group` — container that sticks N Buttons together with collapsed adjacent borders.
- `card` — surface container with optional header / body / footer slots.
- `checkbox` — native HTML5 <input type="checkbox"> styled with kit tokens. **[JS: checkbox-indeterminate]**
- `collapsible` — disclosure widget (single show/hide region). **[JS: collapsible]**
- `divider` — visual separator with an optional center slot.
- `empty` — empty-state surface for "no data / first-time / no results / 404".
- `field` — composes Label + one of the four text controls + optional help text + optional error message.
- `icon` — inline SVG renderer for the curated Lucide v0.469.0 subset.
- `input` — native HTML5 input styled with kit tokens.
- `input-group` — wrapper that sticks a prefix and/or a suffix onto an Input. **[JS: input-group]**
- `item` — a selectable list row.
- `kbd` — keyboard chord visual.
- `label` — visible <label> for a form input.
- `native-select` — native HTML <select> styled with kit tokens.
- `progress` — task progress indicator with two modes:.
- `radio-group` — N native <input type="radio"> sharing the same `name=`, wrapped in a <fieldset><legend>.
- `separator` — semantic or decorative divider, horizontal or vertical.
- `skeleton` — pure-CSS shimmer placeholder.
- `slider` — native <input type="range"> styled with kit tokens.
- `spinner` — pure-SVG rotating arc.
- `switch` — native <input type="checkbox" role="switch"> with a thumb-on-rails visual envelope.
- `textarea` — native HTML5 multi-line field, optionally auto-resizing. **[JS: textarea-autosize]**
- `toggle` — <button type="button" aria-pressed> "press and stay" button. **[JS: toggle]**
- `toggle-group` — N Toggles in a `role="group"` container with arrow-key roving focus + optional mutex (single) / independent (multi) selection. **[JS: toggle-group{%, if, controllerExtras, %}, {{, }}{%, endif]**
- `tooltip` — hover/focus-triggered descriptive popup. **[JS: tooltip]**
- `typography` — wraps `--typography-*` shorthand tokens with optional color / weight / align / tabular-numerals overrides.

## Ring 2 — Compounds

- `alert-dialog` — destructive-confirm wrapper around Dialog. **[JS: dialog]**
- `avatar-group` — visual stack of N Avatars with overflow indicator.
- `calendar` — month-grid date display. **[JS: calendar]**
- `carousel` — slide carousel with optional autoplay + touch swipe + keyboard nav. **[JS: carousel]**
- `chart` — ApexCharts-wrapped data visualization. **[JS: chart]**
- `combobox` — Search-as-you-type single-select listbox with an Input trigger + aria-autocomplete="list" (WAI-ARIA APG "Combobox with listbox popup"). **[JS: popover, combobox]**
- `command` — ⌘K Command palette. **[JS: dialog, combobox, command]**
- `context-menu` — DropdownMenu's twin, opened by right-click (`contextmenu` event) at the pointer coordinates instead of click on a visible trigger. **[JS: popover, dropdown-menu, context-menu]**
- `data-table` — the kit's fully-featured workhorse compound for tabular data. **[JS: {{, (['data-table'], |, merge(controllerExtras)), join(', '), }}]**
- `date-picker` — Input-anchored single-date picker with Calendar popover. **[JS: popover, date-picker]**
- `description-list` — semantic <dl><dt><dd> list of term/definition pairs.
- `dialog` — modal native `<dialog>` element. **[JS: dialog{%, if, controllerExtras, %}, {{, }}{%, endif]**
- `drawer` — modal side-panel using native <dialog>, sliding from a viewport edge. **[JS: drawer{%, if, controllerExtras, %}, {{, }}{%, endif]**
- `dropdown-menu` — Popover-positioned menu of Items. **[JS: popover, dropdown-menu{%, if, controllerExtras, %}, {{, }}{%, endif]**
- `file-upload` — Drop zone + multi-file + preview + progress + cancel. **[JS: file-upload]**
- `hover-card` — Popover variant opened by hover (with delay) for visual previews. **[JS: popover, hover-card]**
- `input-otp` — N-digit one-time-code input (4-8 digits, default 6). **[JS: input-otp]**
- `menubar` — Horizontal app-style nav bar of N DropdownMenu submenus (File / Edit / View / Help). **[JS: menubar]**
- `navigation-menu` — Horizontal web-navigation menu. **[JS: navigation-menu]**
- `number-input` — touch-friendly +/- stepper around a native <input type="number">. **[JS: number-input]**
- `pagination` — Page navigation with first/prev/next/last + inline page-size. **[JS: pagination]**
- `popover` — floating panel anchored to a trigger. **[JS: popover]**
- `resizable` — 2-pane resizable container with drag handle. **[JS: resizable]**
- `scroll-area` — Custom-styled scrollable container.
- `select` — Single-select listbox via Popover-positioned dropdown, styled to look like an Input (border + radius + height match). **[JS: popover, select]**
- `sheet` — responsive variant of Drawer. **[JS: sheet]**
- `sidebar` — vertical application navigation with collapsible icon-only mode. **[JS: sheet]**
- `sonner` — toast viewport markup. **[JS: sonner]**
- `status` — labelled status indicator with optional pulse.
- `stepper` — step-by-step progress indicator (declarative, consumer-driven). **[JS: stepper]**
- `table` — Pure-CSS HTML <table> structural primitive.
- `tabs` — accessible tab interface with WAI-ARIA APG "Tabs" semantics. **[JS: tabs]**
- `tag` — labelled chip with optional iconLeading and opt-in dismiss button. **[JS: tag-dismiss]**
- `toast` — re-export of the Sonner viewport.

## Ring 3 — Patterns

- `activity-feed` — time-bucketed activity stream.
- `auth-account-locked` — terminal account-lock-out pattern.
- `auth-email-verification` — terminal "check your email" pattern.
- `auth-forgot-password` — password-reset request pattern.
- `auth-login` — login pattern.
- `auth-magic-link` — terminal passwordless-login confirmation.
- `auth-otp` — 6-digit verification-code pattern.
- `auth-password-strength` — password field with live strength meter. **[JS: password-strength]**
- `auth-register` — registration pattern.
- `auth-reset-password` — reset-password pattern.
- `auth-session-timeout-modal` — modal with live countdown to session expiry (Ring 3 S3.1b — 2nd new Ring 3 controller). **[JS: dialog, session-timeout-, countdown]**
- `auth-social-buttons` — composable block of OAuth provider buttons.
- `auth-sso` — enterprise SSO entry pattern.
- `back-to-top` — floating back-to-top button (Ring 3 S3.2).
- `chatbot` — conversational UI panel. **[JS: drawer, chatbot]**
- `danger-zone` — destructive-action affordance (Ring 3 S3.3b-1).
- `density-switcher` — comfortable / cozy / compact density picker (Ring 3 S3.3b-1). **[JS: toggle-group, density-switcher]**
- `error-401` — "Authentication required" page.
- `error-403` — "Access denied" page.
- `error-404` — "Page not found" page.
- `error-500` — "Server error" page (Ring 3 S3.1c).
- `error-503` — "Service unavailable" page (Ring 3 S3.1c).
- `error-browser-unsupported` — "Unsupported browser" page.
- `error-coming-soon` — "Feature coming soon" page (Ring 3, brand-feel).
- `error-maintenance` — "Maintenance in progress" page (Ring 3, brand-feel).
- `error-offline` — "No connection" page (Ring 3, brand-feel).
- `error-rate-limited` — "Too many requests" page.
- `footer-mega` — site-level multi-column footer.
- `form-address-autocomplete` — debounced async address autocomplete (sibling Form pattern). **[JS: popover, address-autocomplete]**
- `form-color-picker` — color selection input (HSV/HSL/RGB/HEX + preset palette + optional alpha). **[JS: popover, color-picker]**
- `form-date-range` — date range pair (start + end) (sibling Form pattern). **[JS: form-date-range]**
- `form-file-upload` — file upload as a Form field (sibling Form pattern). **[JS: file-upload]**
- `form-phone-input` — international phone input (intl-tel-input lazy ~25 kB). **[JS: phone-input]**
- `form-signature` — inline signature pad (canvas + lazy signature_pad lib). **[JS: signature-pad]**
- `form-tag-input` — chip-style tag input (Tagify lazy ~12 kB). **[JS: tag-input]**
- `form-with-steps` — multi-step form wizard (foundation pattern of the Forms family). **[JS: stepper, form-with-steps]**
- `keyboard-shortcuts-overlay` — full-screen shortcuts modal (Ring 3 S3.3b-1).
- `lang-switcher` — locale picker (Ring 3 S3.3b-1). **[JS: popover, dropdown-menu, lang-switcher]**
- `nav-bottom-mobile` — canonical iOS / Material bottom tab bar.
- `nav-header` — page-level top navigation pattern. **[JS: drawer]**
- `nav-mega-menu` — multi-column dropdown panel for Nav-Header items. **[JS: navigation-menu, popover]**
- `nav-mobile-drawer` — mobile slide-from-edge nav drawer. **[JS: drawer]**
- `nav-sidebar-app` — app-level vertical nav (Notion / Linear / Vercel pattern).
- `notification-center` — slide-over notifications panel.
- `onboarding-checklist` — setup checklist with progress meter.
- `onboarding-product-tour` — anchored hotspots + popover tour. **[JS: product-tour]**
- `onboarding-welcome-screens` — multi-step welcome flow post-Register.
- `rgpd-cookie-banner` — RGPD cookie consent banner (Ring 3 S3.3b-2). **[JS: dialog, cookie-banner]**
- `rgpd-data-export` — RGPD data portability affordance (Ring 3 S3.3b-2).
- `rgpd-preferences-center` — RGPD granular consent preferences (Ring 3 S3.3b-2). **[JS: dialog, preferences-center]**
- `roles-matrix` — Ring 3 pattern. **[JS: roles-matrix]**
- `search-command-palette` — global ⌘K command palette wrapper (Ring 3 S3.3b-2).
- `search-faceted-filters` — facet filter panel (Ring 3 S3.3b-2). **[JS: faceted-filters]**
- `search-results` — search results list (Ring 3 S3.3b-2).
- `theme-switcher` — light / dark / system theme picker (Ring 3 S3.3b-1). **[JS: toggle-group, theme-switcher]**

## Internal partials

_Shared shells used by other components — not consumed directly._

- `_auth-shell` — centered viewport-min-height layout shell for Auth patterns.
- `_error-shell` — centered viewport-min-height layout shell for Error patterns.
